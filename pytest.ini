[pytest]
# `pythonpath = .` puts the project root on sys.path so `import app` works.
pythonpath = .
testpaths = tests
addopts = -q
markers =
    e2e: end-to-end browser tests (need a running server + Playwright browsers)
