# Development / testing dependencies.
# `-r requirements.txt` means "also install everything the app needs".
-r requirements.txt
httpx>=0.27              # HTTP client used by FastAPI's TestClient (API tests)
pytest>=8               # test runner
pytest-cov>=5           # test coverage reports
ruff>=0.5               # fast linter (used in CI)
pytest-playwright>=0.5  # Playwright end-to-end tests (Phase 3)
locust>=2.20            # load-test smoke (Phase 5)
